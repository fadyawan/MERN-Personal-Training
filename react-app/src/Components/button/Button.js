import * as React from 'react';

const Button = () => {
    const buttonHandler = () => {

    }

    return (
        <div>
            <button type="button" onClick={buttonHandler}>
                Click Here
            </button>
        </div>
    );
};

export default Button;